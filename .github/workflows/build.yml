name: Pyinstaller Build Test
run-name: ${{ github.actor }} is building python code.
on: [push]
jobs:

  linux:
    runs-on: ubuntu-latest
    env:
      DEPL_EXE_NAME: 'linux_x86_64'
    steps:
    - uses: actions/checkout@v4
    - uses: sayyid5416/pyinstaller@v1
      with:
        python_ver: '3.12'
        pyinstaller_ver: '==6.10.0'
        spec: 'src/example.py'
        requirements: 'src/requirements.txt'
        upload_exe_with_name: "$DEPL_EXE_NAME"
        options: --onefile, --name "Example App"

  windows:
    runs-on: windows-2019
    env:
      DEPL_EXE_NAME: 'windows_x86_64'
    steps:
    - uses: actions/checkout@v4
    - uses: sayyid5416/pyinstaller@v1
      with:
        python_ver: '3.12'
        pyinstaller_ver: '==6.10.0'
        spec: 'src/example.py'
        requirements: 'src/requirements.txt'
        upload_exe_with_name: "$DEPL_EXE_NAME"
        options: --onefile, --name "Example App"

  macos:
    strategy:
      matrix:
        os-version: ['12', '14']
        include:
          - os-version: '12'
            arch: x86_64
          - os-version: '14'
            arch: aarch64
    runs-on: macos-${{ matrix.os-version }}
    env:
      # even though we run on 12, target towards compatibility
      MACOSX_DEPLOYMENT_TARGET: '11.0'
      DEPL_EXE_NAME: macos_${{ matrix.arch }}'
    steps:
    - uses: actions/checkout@v4
    - uses: sayyid5416/pyinstaller@v1
      with:
        python_ver: '3.12'
        pyinstaller_ver: '==6.10.0'
        spec: 'src/example.py'
        requirements: 'src/requirements.txt'
        upload_exe_with_name: "$DEPL_EXE_NAME"
        options: --onefile, --name "Example App"